package templating

// Functions in this file should be able to take the resource objects and
// create a file that contains the respective functions to return the
// objects

// ResourceObjectToReturnFile ...
func ResourceObjectToReturnFile(resource *interface{}) (string, error) {
	return "", nil
}
